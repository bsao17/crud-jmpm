{% extends 'base.html.twig' %}

{% block title %}Edit Billet{% endblock %}

{% block body %}
    <div class="container border border-3 border-light bg-dark text-light">
        <h2 class="text-center bg-secondary text-dark">Edit Billet</h2>

        {{ include('billet/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('billet_index') }}" class="btn btn-sm btn-warning m-1">back to list</a>

        {{ include('billet/_delete_form.html.twig') }}
    </div>
{% endblock %}
